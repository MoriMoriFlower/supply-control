# SupplyControl 固有指示（CLAUDE.md）

医薬品の供給不安を「通知」で受け取るPWA。
上位の `薬剤師/CLAUDE.md` とグローバル `~/.claude/CLAUDE.md` に**追加**で適用する。

- **リポジトリ：** <https://github.com/MoriMoriFlower/supply-control>（**public**・2026-08-16作成）
- **Cloudflareは未接続**（Step 2 で Git連携型として繋ぐ）

---

## 1. 承認済みサイト（再確認不要）

| サイト | 用途 | 承認日 |
|---|---|---|
| `www.mhlw.go.jp` | 供給状況一覧表xlsxの取得（一次情報源） | 2026-08-16 |

- **`drugshortage.jp`（DSJP）はアクセスしない。** WebFetchが403＝機械的アクセスを拒否している。一次情報の厚労省xlsxだけを使う
- 上記以外への外部アクセスは、グローバル規則2に従って**都度許可を得る**

---

## 2. 決定事項（★は覆さない）

- **★気になる薬リストはサーバーに送らない。** 端末内（IndexedDB）に置き、突合も通知文の組み立ても端末側でやる。
  サーバーが預かるのは**プッシュ購読情報（匿名文字列）だけ**。氏名・メール・所属・患者情報・薬リストは一切預からない
  - 理由と背景は共通ノウハウ **I-1**。採用薬リストは勤務先の調達情報にあたる
  - **これを台無しにする実装を足さない**：通知の開封計測／「該当◯件」のサーバー返送／エラーログへのリスト混入
  - **localStorageは使えない。** Service Workerは同期APIを呼べないため。画面側も同じIndexedDBを読む（共通ノウハウ C-6 周辺）
- **★リストの呼称は「気になる薬」。**「採用薬」と呼ばない（I-1）。ただし呼び方でリスクは減らないので、端末内完結の設計は厳守
- **★重い処理はGitHub Actions、Cloudflareは配るだけ。** CPU 10msでは16,384行のxlsx解析は絶対に終わらない（共通ノウハウ A-11）
- **★既存の検索アプリには組み込まない。** 更新頻度も配信方式も性格も違う。連携（YJコードで飛ばす／リストの受け渡し）は後付けする
- **★通知は「変化した事実」だけを伝え、判断は一次情報リンクに飛ばす。** 在庫・代替・処方変更について何も言わない
- **合言葉はつけない。ただし公開までは検索避け**（`robots.txt` / `<meta name="robots">` / `_headers` の `X-Robots-Tag`）。
  公開に踏み切るときは**3箇所とも外す**必要がある
- **リポジトリはpublic。** 秘密（VAPID秘密鍵・Cloudflare APIトークン）は必ず GitHub Secrets。コードにも `data/` にも置かない

---

## 3. 元データについて（2026-08-16 実測）

出典：厚労省「医療用医薬品の供給状況」ページ → `.xlsx` 直リンク1本。

| 項目 | 実測値（令和8年8月14日版） |
|---|---|
| シート | 「供給状況一覧表」1枚のみ |
| 行 | ヘッダーR02、データR03から。**16,384行** |
| 列 | 21列 |
| ⑤YJコード | **空0件・重複0件**＝主キーとして使える |
| ⑫出荷対応 | 通常14,542 / 供給停止873 / 限定出荷(自社)479 /(他社影響)416 /(その他)74 |
| ⑰出荷量 | 通常10,711 / 増加3,457 / 減少1,044 / 薬価削除予定699 / 出荷停止473 |
| ファイルサイズ | 1.48MB |

### 罠（踏んだもの／踏みかけたもの）

- **㉑「今回掲載時の更新有無(New)」は差分検出に使えない。** 実測でNewは3件のみ。「新規掲載」の意味であって「ステータス変化」ではない → 共通ノウハウ **F-7**
- **⑬「⑫を更新した日」は使える。** 実測3,446件に日付が入っており、令和7年5月13日以降の変化履歴が読める。
  **ただし「最後に更新した日」なので上書きされる**＝古い日ほど過小評価。日次の完全な差分は自前で貯めるしかない
- **ファイル名の数字は西暦の下2桁。** `260814iyakuhinkyoukyu.xlsx` = 2026-08-14。
  和暦（令和8年）ではない。`resolveSource()` はリンク文言の和暦とファイル名の西暦を**両方読んで一致を検証**し、食い違えば止める
- **YJコードが12桁でないものが404件ある。** すべて `X00000` 形式の未収載医薬品（液化酸素などの医療ガス）で、全件「①通常出荷」。
  **既存の検索アプリ（PMDA電子添文＝12桁YJ）とは結合できない**。異常ではないので止めないが、件数を `meta.json` に残している
- **厚労省は最新版1本しか置かない。** 過去分のリンクは無い → git履歴が唯一の過去データ（F-7）
- **土日は更新されない。** ⑬の記録291日ぶんに土日は1日も無い

### 1日あたりの変化件数（⑬から推定・2026-08-16時点）

直近3か月で **中央値12件／平均11.8件／最大54件**（記録がある日のみ・平日のみ）。曜日別では木曜が多い（平均21.6件）。
**⑬は上書きされるため下限寄りの推定**。運用開始後に実測値へ置き換えること。

---

## 4. 実装の掟

- **列構成が変わったら黙って動かない。** `scripts/lib/schema.mjs` に原本の見出しを逐語で持ち、毎回突き合わせて不一致なら例外で止める。
  「別の列を保存し続ける」ほうが「止まる」より遥かに悪い（共通ノウハウ E-13）
- **不可視文字（NUL・ゼロ幅・BOM）は正規化の最初に落とす。** ソースコードには**エスケープ表記でしか書かない**（直接埋めると目視で判別できない）→ E-7
- **`data/*.json` は1行1レコードで書く。** gitのdiffが「変わった品目の行だけ」になり、履歴が軽くなる
- **差分の並び順を固定する。** 順序が揺れると、中身が同じでも毎日diffが出てコミットが無駄に増える
- **`npm run selftest` は外部アクセス不要**で動く。差分ロジックを触ったら必ず実行する（Actionsでも毎回走る）

---

## 5. 未確認・これから確かめること

- **iOSのWeb Push実機検証**（共通ノウハウ C-6。仕様調査のみで実機未検証）
  - ホーム画面に追加したPWAでのみ届くか
  - 許可ダイアログがタップ起点でのみ出るか
  - **該当0件の日に沈黙できないという想定が本当か**（`userVisibleOnly` の実挙動）
- 新システム `iyakuhin-kyokyu.mhlw.go.jp/public/supply-status-list`（令和8年3月31日稼働）にAPI/CSVがあるか。
  SPAのため未取得。**現行xlsxが廃止される可能性があるので、いずれ確認が要る**
- 元ファイルが実際に更新される時刻（Last-Modifiedの観測を貯めて、cronの時刻を最適化する）
  - **観測1件目：** 2026-08-14版 = `Fri, 14 Aug 2026 05:18:41 GMT` ＝**JST 14:18**。
    現状の cron（JST 16:10 / 20:10）はこの後ろなので妥当。数件貯まったら見直す
