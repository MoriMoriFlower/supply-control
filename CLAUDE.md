# SupplyControl 固有指示（CLAUDE.md）

医薬品の供給不安を「通知」で受け取るPWA。
上位の `薬剤師/CLAUDE.md` とグローバル `~/.claude/CLAUDE.md` に**追加**で適用する。

- **リポジトリ：** <https://github.com/MoriMoriFlower/supply-control>（**public**・2026-08-16作成）
- **Cloudflareは未接続**（Step 2 で Git連携型として繋ぐ）

---

## 1. 承認済みサイト（再確認不要）

| サイト | 用途 | 承認日 |
|---|---|---|
| `www.mhlw.go.jp` | 供給状況一覧表xlsxの取得（一次情報源） | 2026-08-16 |
| `iyakuhin-kyokyu.mhlw.go.jp` | 医薬品安定供給状況等管理システム（新システム・§6）。**カナリアが毎回叩く** | 2026-08-17 |
| `gbiz-id.go.jp` | GビズID（デジタル庁）の発行条件の確認（§6）。**ハイフンあり**が正 | 2026-08-17 |

- **`drugshortage.jp`（DSJP）はアクセスしない。** WebFetchが403＝機械的アクセスを拒否している。一次情報の厚労省xlsxだけを使う
- 上記以外への外部アクセスは、グローバル規則2に従って**都度許可を得る**

---

## 2. 決定事項（★は覆さない）

- **★気になる薬リストはサーバーに送らない。** 端末内（IndexedDB）に置き、突合も通知文の組み立ても端末側でやる。
  サーバーが預かるのは**プッシュ購読情報（匿名文字列）だけ**。氏名・メール・所属・患者情報・薬リストは一切預からない
  - 理由と背景は共通ノウハウ **I-1**。採用薬リストは勤務先の調達情報にあたる
  - **これを台無しにする実装を足さない**：通知の開封計測／「該当◯件」のサーバー返送／エラーログへのリスト混入
  - **localStorageは使えない。** Service Workerは同期APIを呼べないため。画面側も同じIndexedDBを読む（共通ノウハウ C-6 周辺）
- **★リストの呼称は「気になる薬」。**「採用薬」と呼ばない（I-1）。ただし呼び方でリスクは減らないので、端末内完結の設計は厳守
- **★重い処理はGitHub Actions、Cloudflareは配るだけ。** CPU 10msでは16,384行のxlsx解析は絶対に終わらない（共通ノウハウ A-11）
- **★既存の検索アプリには組み込まない。** 更新頻度も配信方式も性格も違う。連携（YJコードで飛ばす／リストの受け渡し）は後付けする
- **★通知は「変化した事実」だけを伝え、判断は一次情報リンクに飛ばす。** 在庫・代替・処方変更について何も言わない
- **合言葉はつけない。ただし公開までは検索避け**（`robots.txt` / `<meta name="robots">` / `_headers` の `X-Robots-Tag`）。
  公開に踏み切るときは**3箇所とも外す**必要がある
- **リポジトリはpublic。** 秘密（VAPID秘密鍵・Cloudflare APIトークン）は必ず GitHub Secrets。コードにも `data/` にも置かない

---

## 3. 元データについて（2026-08-16 実測）

出典：厚労省「医療用医薬品の供給状況」ページ → `.xlsx` 直リンク1本。

| 項目 | 実測値（令和8年8月14日版） |
|---|---|
| シート | 「供給状況一覧表」1枚のみ |
| 行 | ヘッダーR02、データR03から。**16,384行** |
| 列 | 21列 |
| ⑤YJコード | **空0件・重複0件**＝主キーとして使える |
| ⑫出荷対応 | 通常14,542 / 供給停止873 / 限定出荷(自社)479 /(他社影響)416 /(その他)74 |
| ⑰出荷量 | 通常10,711 / 増加3,457 / 減少1,044 / 薬価削除予定699 / 出荷停止473 |
| ファイルサイズ | 1.48MB |

### 列の一覧（原本の見出しを逐語で・全21列）

正本は `scripts/lib/schema.mjs` の `COLUMNS`。**ここは読む人のための写しなので、食い違ったら schema.mjs が正しい。**
「群」は保存先（`item`＝品目の素性・ほぼ変わらない／`status`＝供給状況・毎日変わりうる）。

| # | 原本の見出し（逐語） | 群 | キー | 補足 |
|---|---|---|---|---|
| ① | 薬剤区分 | item | `kubun` | 内用薬／注射薬 等 |
| ② | 薬効分類（保険薬収載時点の薬効分類を記載） | item | `yakko` | |
| ③ | 成分名 | item | `seibun` | 検索対象 |
| ④ | 規格単位※全角 | item | `kikaku` | |
| ⑤ | YJコード | item | `yj` | **★主キー。**空0件・重複0件 |
| ⑥ | 品名（承認書に記載の正式名称）※全角 | item | `hinmei` | 検索対象・通知の主語 |
| ⑦ | 製造販売業者名 | item | `maker` | 検索対象 |
| ⑧ | 製品区分 | item | `seihinKubun` | 先発／後発／その他 等 |
| ⑨ | 基礎的医薬品 | item | `kisoteki` | |
| ⑩ | （重要）供給確保医薬品 | item | `kakuho` | |
| ⑪ | 薬価収載年月日 | item | `yakkaDate` | 日付 |
| ⑫ | 製造販売業者の「出荷対応」の状況 | status | `shukka` | **★通知の主軸。**下に値一覧 |
| ⑬ | 当該品目の⑫の情報を更新した日（本項目を報告内容として追加した令和7年5月13日以降に⑫の情報を更新した品目についてのみ記載） | status | `shukkaUpdated` | 日付。**上書きされる**ので過去は過小評価 |
| ⑭ | 限定出荷/供給停止の理由 | status | `riyu` | 画面には出すが通知しない |
| ⑮ | 限定出荷の解除見込み／供給停止の解消見込み | status | `kaijo` | 同上 |
| ⑯ | 限定出荷の解除見込み／供給停止の解消見込み／販売中止品の在庫消尽時期 | status | `shoujin` | 同上 |
| ⑰ | 製造販売業者の「出荷量」の現在の状況 | status | `ryo` | **★⑫と独立。**下に値一覧 |
| ⑱ | 製造販売業者の「出荷量」の改善（増加）見込み時期 | status | `kaizenJiki` | 通知しない |
| ⑲ | ⑱を任意選択した場合の「出荷量」の改善（増加）見込み量 | status | `kaizenRyo` | 通知しない |
| ⑳ | 当該品目の⑫以外の情報を更新した日 | status | `otherUpdated` | 日付 |
| （㉑） | 今回掲載時の更新有無（更新有りの場合、Newと表示） | status | `isNew` | **★原本の見出しに丸数字は無い。**「㉑」はこちらが便宜上付けた呼び名。差分検出には使えない（F-7） |

**⑫出荷対応に入る値（5種・原本の表記どおり）**
`①通常出荷` ／ `②限定出荷（自社の事情）` ／ `③限定出荷（他社品の影響）` ／ `④限定出荷（その他）` ／ `⑤供給停止`

**⑰出荷量に入る値（5種・原本の表記どおり）**
`A．出荷量通常` ／ `Aプラス．出荷量増加` ／ `B．出荷量減少` ／ `C．出荷停止` ／ `D．薬価削除予定`

- ⑫にも⑰にも**丸数字やアルファベットが値そのものに含まれている**（`①通常出荷` で1つの文字列）。前方一致で判定するときはこれを前提にしている
- **⑫の①〜⑤と、列番号の①〜⑳は別物。**「⑫の⑤」＝供給停止であって5列目ではない

### ⑫出荷対応 × ⑰出荷量（2026-08-16 実測・令和8年8月14日版）

**この2列は独立している。片方だけ見ると見落とす。**

| ⑫出荷対応 | ⑰出荷量 | 件数 |
|---|---|---|
| ①通常出荷 | A．通常 | 10,240 |
| ①通常出荷 | Aプラス．増加 | 3,205 |
| **①通常出荷** | **B．減少** | **827** |
| ①通常出荷 | D．薬価削除予定 | 270 |
| ⑤供給停止 | C．出荷停止 | 473 |
| ⑤供給停止 | D．薬価削除予定 | 400 |
| ②③④限定出荷 | A．通常 ／ Aプラス．増加 | 723 |
| ②③④限定出荷 | B．減少 | 217 |
| ②③④限定出荷 | D．薬価削除予定 | 29 |

- **★出荷量減少1,044件のうち827件（79%）は「①通常出荷」。** メーカーの回答は通常出荷でも、量は減っている。
  **⑫だけで「調整中」を判定すると、現場で一番効くこの層が丸ごと抜ける**
- **①通常出荷なのに薬価削除予定が270件。** 「今は普通に来るが、いずれ無くなる」もの
- **C．出荷停止(473)は全件⑤供給停止**＝⑰Cは⑫⑤の部分集合。ここだけは重なっている

### 罠（踏んだもの／踏みかけたもの）

- **㉑「今回掲載時の更新有無(New)」は差分検出に使えない。** 実測でNewは3件のみ。「新規掲載」の意味であって「ステータス変化」ではない → 共通ノウハウ **F-7**
- **⑬「⑫を更新した日」は使える。** 実測3,446件に日付が入っており、令和7年5月13日以降の変化履歴が読める。
  **ただし「最後に更新した日」なので上書きされる**＝古い日ほど過小評価。日次の完全な差分は自前で貯めるしかない
- **ファイル名の数字は西暦の下2桁。** `260814iyakuhinkyoukyu.xlsx` = 2026-08-14。
  和暦（令和8年）ではない。`resolveSource()` はリンク文言の和暦とファイル名の西暦を**両方読んで一致を検証**し、食い違えば止める
- **YJコードが12桁でないものが404件ある。** すべて `X00000` 形式の未収載医薬品（液化酸素などの医療ガス）で、全件「①通常出荷」。
  **既存の検索アプリ（PMDA電子添文＝12桁YJ）とは結合できない**。異常ではないので止めないが、件数を `meta.json` に残している
- **厚労省は最新版1本しか置かない。** 過去分のリンクは無い → git履歴が唯一の過去データ（F-7）
- **土日は更新されない。** ⑬の記録291日ぶんに土日は1日も無い
- **★ただし「毎営業日」でもない。** 直近90日の平日65日のうち、⑬に記録があるのは**48日＝74%**。
  お盆は特に止まる（2026年は 8/10月=2件・8/12水=1件・8/14金=3件。8/11は山の日、8/13は記録なし）。
  **「今日は変化なし」は障害ではなく通常。** 数日コミットが出なくても壊れたと判断しない
  （⑬は上書きされるので74%は下限。実際の更新日はもう少し多い可能性がある）

### 1日あたりの変化件数（⑬から推定・2026-08-16時点）

直近3か月で **中央値12件／平均11.8件／最大54件**（記録がある日のみ・平日のみ）。曜日別では木曜が多い（平均21.6件）。
**⑬は上書きされるため下限寄りの推定**。運用開始後に実測値へ置き換えること。

---

## 3.5 ファイル構成と、配るもの／配らないもの（2026-08-16 Step 2 で追加）

```
scripts/collect.mjs      厚労省xlsx取得 → items.json / status.json / changes/ / meta.json
scripts/build-index.mjs  保存済みJSON → search.json / status-lite.json（外部アクセス不要）
scripts/recompute.mjs    保存済み changes/ の通知フラグを、いまの判定で付け直す（下記）
scripts/build-site.mjs   dist/ を組み立てる（pwa/ ＋ 画面が使う data/ だけ）
scripts/dev.mjs          手元で見るための静的サーバー（127.0.0.1:8787）
scripts/make-icons.mjs   SVG→PNGアイコン生成（手動・mutool依存・buildからは呼ばない）
scripts/lib/canary.mjs   新システムAPIとの突合＝xlsxが静かに止まったことに気付くための見張り（§6）
pwa/                     画面。素のESM・依存ライブラリなし
```

| コマンド | すること |
|---|---|
| `npm run collect` | 厚労省から取得（**外部アクセスあり**。ついでにカナリアが新システムAPIを3回叩く） |
| `npm run index` | 索引を作り直す。外部アクセスなし・いつでも実行可 |
| `npm run recompute` | 保存済みの通知フラグを付け直す。外部アクセスなし・冪等 |
| `npm run selftest` | 差分・検索・索引・カナリアの自己テスト（外部アクセスなし・56項目） |
| `npm run icons` | アイコンPNGを作り直す。**絵柄を変えたときだけ**手で回す（下記） |
| `npm run build` | `dist/` を組み立て |
| `npm run dev` | build してローカル配信 → <http://localhost:8787> |

### アイコン（2026-08-17 作成）

- **★iOSは manifest の `icons` を見ない。** ホーム画面のアイコンは `index.html` の
  `<link rel="apple-touch-icon">`（PNG）だけで決まる。無いとページのスクショになる
- **元は2枚のSVG。** `icon.svg`（角丸あり）と `icon-full.svg`（全面塗り・角丸なし）。
  **角丸は自分で付けてはいけない用途がある**：apple-touch-icon はiOSが、maskable はAndroidが
  それぞれ自前のマスクを被せるので、こちらで角丸にすると角に隙間が出る／余白が白く抜ける
- **maskableの安全域**＝中心から半径 512×0.4＝204.8 の円。現在の絵柄の最遠点は176で収まっている。
  **絵柄を変えたら再計算すること**（`icon-full.svg` の冒頭コメントに計算を残してある）
- 生成は **mutool**（MuPDF）。単色の塗りだけなので共通ノウハウ F-3（グラデーションが消える）は該当しない。
  **日本語パスでも動く**ことを実測で確認済み（LibreOfficeとは違う）
- `npm run icons` は **buildからもActionsからも呼ばない**。ランナーにmutoolが無いため。
  PNGはコミット済みなので普段は実行不要

**Cloudflare Pages（Git連携型）の設定：** ビルドコマンド `npm run build` ／ 出力ディレクトリ `dist`

### 配布物の役割（★分けている理由を崩さない）

| ファイル | 生 | brotli | 変わる頻度 | 配信 |
|---|---|---|---|---|
| `search.json` 品目の素性 | 2,242KB | 196KB | **まれ**（新規収載・掲載終了のときだけ） | する |
| `status-lite.json` 出荷対応・出荷量（コード） | 350KB | 42KB | 毎日 | する |
| `meta.json` 版と各ファイルのsha256 | 1.6KB | 0.7KB | 毎日 | する |
| `changes/*.json` 変化した品目 | 数KB | — | 毎日 | する |
| `items.json` / `status.json` 原本の全列 | 5.6MB | 320KB | 毎日 | **しない** |

- **★索引に版（`asOf`）を書かない。** 書くと中身が同じ日でもsha256が変わり、
  2.2MBを毎日取り直すことになる。版は `meta.json` が持つ（共通ノウハウ **A-13**）
- **PWAは `meta.json` のsha256を見て、変わっていなければ端末の控え（IndexedDB）を使う。**
  ふだんの起動は数十KBで済む
- **`items.json` / `status.json` は配らない。** 画面が読まないため。
  理由・見込みまで表示する画面を作るときに、そのとき必要なものを `build-site.mjs` の `SHIP` に足す。
  git履歴には残り続けるので失われはしない
- **「重い」と言う前に圧縮後を測る。** 日本語JSONはbrotliで16分の1になる（共通ノウハウ **A-12**）

### 通知に出す変化（`scripts/lib/diff.mjs` の `isNotifiable`）

「画面に出す変化」と「通知する変化」は別。全部通知するとノイズで読まれなくなる。

| 変化 | 画面 | 通知 |
|---|---|---|
| ⑫出荷対応が変わった | ○ | **する** |
| 掲載終了 | ○ | **する** |
| ⑰出荷量が変わった（**A ⇄ Aプラス 以外すべて**） | ○ | **する**（悪化も回復も） |
| ⑰出荷量が A ⇄ Aプラス | ○ | しない |
| 新規掲載（⑫が①でない、または⑰が普通でない） | ○ | **する** |
| 新規掲載（⑫①かつ⑰A/Aプラス） | ○ | しない |
| ⑭理由・⑮⑯見込みだけの更新 | ○ | しない |

- **★⑰出荷量の変化を通知から外さない（2026-08-17決定）。** 旧実装は `kind === 'ryo'` を一律 `false` にしていた。
  ⑫が①通常出荷のまま⑰だけBに落ちる品目が実測827件あり、**この層が丸ごと沈黙していた**。
  初日に現物が出た：**ユナスピン静注用1.5g（アンピシリン・スルバクタム）が ⑫①のまま A→B、旧実装では通知0件**
- **★黙らせるのは A ⇄ Aプラス だけ。** 現場から見ればどちらも「発注すれば入る」。
  実測 2026-08-17 は⑰の変化8件中5件がこれで、通知するとノイズにしかならない
- **★B・C・D はそれぞれ別物として扱う。** `B．減少 → C．出荷停止`（減っている→止まった）や
  `B．減少 → D．薬価削除予定`（いずれ無くなる）も通知する。
  **一度これを「異常どうしの移動だから」と黙らせる実装にして、B→Cが沈黙する穴を作った（2026-08-17・同日修正）。**
  ⑰は5段階のうちA系だけが「入る」であって、残り3つを一括りにしてよい理由はない
- **知らない⑰の値が来たら、それ自体を1区分として扱う**（`schema.mjs` の `RYO_NORMAL` に無いもの）。
  普通↔知らない値の移動は必ず通知される。ただし**空欄は「未報告」なので普通扱い**
- 新しい判定は `npm run selftest` の⑦⑧節が見張っている。**落ちたら沈黙が戻ったということ**

#### ★通知フラグは「焼き付け」なので、判定を直したら付け直す（2026-08-17 実際に踏んだ）

`notify` は collect が走った瞬間の判定を `data/changes/*.json` に**書き込んで固定**したもの。
判定（`isNotifiable`）を直しても、保存済みのファイルは古い判定のまま残る。
しかも **collect は元ファイルが変わらない日は「変化なし」で早期終了する**ので、次の更新日まで直りが反映されない。

- **実際にこうなった：** ⑰出荷量を通知するよう直した当日、`data/changes/2026-08-17.json` は `notify: 0` のままだった。
  **直したはずのユナスピン静注用1.5g（⑫①のまま A→B）が、記録の上ではまだ沈黙していた**
- **直し方：`npm run recompute`。** 外部アクセスなし・何度実行しても同じ（冪等）。
  判定は持たず `diff.mjs` の `isNotifiable` をそのまま呼ぶ（判定を2箇所に書くと必ず食い違う）
- **見張り：`npm run selftest` の⑬節**が、保存済みの全フラグを現在の判定と突き合わせる。
  **落ちたら `npm run recompute` を実行してコミットする**

### カナリア（新システムAPIとの突合・2026-08-17 実装）

**xlsxが廃止されると collect は「前回と同一ファイル → 変化なし」で静かに成功し続ける。**
何も壊れていないように見えたまま、通知だけが永久に来なくなる。これに気付くための唯一の仕掛け。
（総則1「0件・変化なしは“結果”ではなく“照合が成立しなかったサイン”」の機械化）

| 見張る対象 | 鳴る条件 | 根拠 |
|---|---|---|
| 件数の乖離 | APIの `total` と行数の差が **100件超** | 1日の変化は中央値12件。桁違いの食い違いだけ拾う |
| 版の遅れ | APIに「こちらの版＋**8日**以降に掲載された品目」がある | xlsxは平日のみ・毎営業日でもない。お盆は数日空く（実測 8/11〜8/13）。1週間丸ごと止まったら休みではない |

- **★カナリアは絶対に本体を止めない。** 非公表APIなので落ちて当然。`runCanary()` は例外を外へ投げず、
  失敗は `{ok:false}` として記録するだけ。**API不調自体はアラートにしない**（毎日鳴ると本当の異常が埋もれる）
- **★「変化なし」で早期終了する経路でも必ず回す。** そこがカナリアの本番だから
- **★Actionsでは「コミットの後ろ」のステップで落とす。** 前に置くと、取れたデータが履歴に残らないまま失敗する。
  collect は `canary_alert` という出力を立てるだけで、自分では落ちない
- 判定部（`judge` / `addDays`）はネットワークを触らない純粋関数にしてあり、selftest ⑭節が直接叩いている
- **2026-08-17 実測：** API 16,385件 / xlsx 16,385件（差0）・版より新しい掲載0件＝正常

### 検索の正規化（`pwa/js/normalize.js`）

原本は全角（`ラボナール注射用０．３ｇ`）。NFKC →小文字→カタカナをひらがなへ→小書きを大書きへ→記号と長音を除去、で突き合わせる。
実測で `ラボナール 0.3g` `らぼなーる` `ﾑｺﾀﾞｲﾝ` `ろきそにん錠60` すべて当たる。

- **既知の副作用：** 記号を落とすので `2.5mg` が `25mg` になり、`5mg` で検索すると当たる。
  **消さずに並べ替えで救っている**（品名の先頭一致＞品名に含む＞短い順）。利用者は一覧から原本の品名を見て選ぶので、誤った情報にはならない
- 項目の区切りは `|`。正規化で必ず消える文字なので、検索語には現れない＝項目をまたいだ誤ヒットが起きない

### しぼり込み（`pwa/js/app.js` の `FILTERS`）

さがすタブで、語を入れずにボタンだけ押すと一覧できる（品名順・100件ずつ）。件数はボタン上に出る。検索語との併用も可。

- **★「調整中」は厚労省の⑫出荷対応の ②③④⑤ をまとめただけ**（限定出荷3種＋供給停止）。
  こちらで独自の基準を作らない。**①通常出荷を調整中に含めない**。実測 1,842件（限定出荷969・供給停止873）
- **★「出荷量減少」は⑰出荷量のB＝別の列**なので、ボタンも仕切って分けてある。
  **1,044件のうち827件は⑫が①通常出荷**＝調整中には出てこない（下のクロス集計）。
  この2列を1つのボタンにまとめない

---

## 4. 実装の掟

- **列構成が変わったら黙って動かない。** `scripts/lib/schema.mjs` に原本の見出しを逐語で持ち、毎回突き合わせて不一致なら例外で止める。
  「別の列を保存し続ける」ほうが「止まる」より遥かに悪い（共通ノウハウ E-13）
- **不可視文字（NUL・ゼロ幅・BOM）は正規化の最初に落とす。** ソースコードには**エスケープ表記でしか書かない**（直接埋めると目視で判別できない）→ E-7
- **`data/*.json` は1行1レコードで書く。** gitのdiffが「変わった品目の行だけ」になり、履歴が軽くなる
- **差分の並び順を固定する。** 順序が揺れると、中身が同じでも毎日diffが出てコミットが無駄に増える
- **`npm run selftest` は外部アクセス不要**で動く。差分ロジックを触ったら必ず実行する（Actionsでも毎回走る）
- **ソースに不可視文字を直接埋めない。** 実際に `normalize.js` へ U+0001 が2つ混入した（2026-08-16）。
  目視では絶対に見つからないので `npm run selftest` が機械で検査している（共通ノウハウ **E-7補**）
- **`pwa/` に外部ライブラリを入れない。** 素のESMだけで書く。
  `_headers` の CSP を `default-src 'self'` / `connect-src 'self'` に絞ってあり、外部への通信は仕組みとして塞いである。
  ★これは「気になる薬リストが外へ出る経路を作らない」ための担保でもあるので緩めない

---

## 5. 未確認・これから確かめること

- **iOSのWeb Push実機検証**（共通ノウハウ C-6。仕様調査のみで実機未検証）
  - ホーム画面に追加したPWAでのみ届くか
  - 許可ダイアログがタップ起点でのみ出るか
  - **該当0件の日に沈黙できないという想定が本当か**（`userVisibleOnly` の実挙動）
- ~~新システムにAPI/CSVがあるか~~ → **2026-08-17に調査完了。§6を参照**
- 元ファイルが実際に更新される時刻（Last-Modifiedの観測を貯めて、cronの時刻を最適化する）
  - **観測1件目：** 2026-08-14版 = `Fri, 14 Aug 2026 05:18:41 GMT` ＝**JST 14:18**
  - **観測2件目：** 2026-08-17版 = `Mon, 17 Aug 2026 08:11:16 GMT` ＝**JST 17:11**
  - **★公開時刻は日によって3時間ずれる（14:18 / 17:11）。「毎日◯時」と決め打ちできない。**
    JST 15:20 の回だけなら 8/17 は取り逃していた＝**1日4回に増やした判断は正しかった**
- **★GitHubのスケジュールは指定時刻に走らない前提で組む。** 2026-08-17 の実測で、
  `cron: '10 7 * * *'`（JST 16:10）に対し**実際の起動は JST 16:58＝48分遅れ**。
  混雑時は遅延・スキップがある。だから1日4回に増やして回数で吸収している
  （publicリポジトリのActionsは無料・1回17秒・変化なしならコミットしないので、増やす副作用がない）
- **⚠ 通知が突然来なくなったら、まずここを疑う：GitHubは「リポジトリに60日間活動がない」とスケジュール実行を自動停止する。**
  本プロジェクトはほぼ毎日コミットが出る想定なので通常は当たらないが、
  **bot（github-actions）のコミットが「活動」として数えられるかは未確認**。
  止まっていたら Actions 画面に再有効化ボタンが出る（`workflow_dispatch` の手動実行でも復帰する）

---

## 6. 新システム「医薬品安定供給状況等管理システム」（2026-08-17 調査）

`https://iyakuhin-kyokyu.mhlw.go.jp` ／ 令和8年3月31日稼働。Vue製のSPA。
`robots.txt` は `Allow: /`（機械的アクセスを禁じていない）。

### ★結論を先に：公式が「うちと同じ機能」を持っている

公開画面 `/public/supply-status-notify-setting` に、原文どおり以下がある：

> **お知らせメールの設定（出荷状況変更通知）**
> 「〜に変化があった際、GビズIDと紐づいたメールアドレスに変更通知メールを配信します」

さらに **お気に入り機能**もある。
つまり **「気になる薬を登録して、変化したらメールで受け取る」は公式に存在する。**

### ★通知の対象は⑫だけ（2026-08-17 確定・画面の原文）

公開画面 `/public/supply-status-notify-setting` の説明文、**逐語**：

> GビズIDと連携することで、医薬品のお気に入り登録が可能となります（最大2,000件）
> また、「お知らせメールの送信」をONにした場合、お気に入り登録した医薬品の
> **出荷対応状況（通常出荷、限定出荷、供給停止）** に変化があった際、
> GビズIDと紐づいたメールアドレスに変更通知メールを配信します

- **「出荷対応状況（通常出荷、限定出荷、供給停止）」＝⑫のみ。⑰出荷量は対象外。** 推測ではなく画面の明文
- したがって **⑫①通常出荷のまま⑰がB．出荷量減少に落ちる 827件は、公式メールでは沈黙する**
  （初日の実例：ユナスピン静注用1.5g）
- **これが SupplyControl の存在意義の中心。** 呼称の違いではなく、拾う列が1本多い

### ★GビズIDの発行条件（2026-08-17・`gbiz-id.go.jp` 一次情報で確認）

**⚠ 正式ドメインは `gbiz-id.go.jp`（ハイフンあり）。`gbizid.go.jp` は存在しない。**

FAQ「誰でもアカウントを作成できますか？」の条件表、**逐語**：

| 種別 | 条件（原文） |
|---|---|
| GビズIDエントリー | 「事業をしている方」「**組織の従業員・職員等の方**」 |
| GビズIDプライム | 「法人番号を有し、かつ、法人番号等を公表することについて同意している企業等の代表者の方」「府省/地方公共団体に所属する決裁権者の方」「事業を営む個人の方」 |
| GビズIDメンバー | 「GビズIDプライムを取得している組織の従業員・職員等の方」 |

- **★前言撤回：勤務先に雇われている薬剤師でも「エントリー」を個人で取れる。**
  以前このファイルに書いた「個人で取れるものではない」は**誤り**（プライムの条件をGビズID全体の条件だと思い込んだ）
- **エントリーの敷居は極めて低い**（クイックマニュアル Ver4.4 より）：
  必要なものは**メールアドレスとパソコンだけ**。印鑑証明書・マイナンバーカード・スマホ・法人番号すべて不要。
  **審査なし・即日発行・有効期限なし**（有効期限2年3か月があるのはプライムとメンバーだけ）
- **★この新システムはエントリーで入れる。** GビズIDの行政サービス一覧に
  「医薬品安定供給・流通確認システム」（`https://iyakuhin-kyokyu.mhlw.go.jp/login`）が
  **「プライム メンバー エントリー」** として載っている
- **未検証：** エントリーでログインした場合に「お気に入り＋お知らせメール」まで使えるかは、
  実際にアカウントを作らないと確認できない。サービス一覧はログイン可否を示すだけ

**うちが残す差（訂正後）：**
1. **★⑰出荷量の変化も拾う。**（上記のとおり**確定**。ここだけが揺るがない差）
2. 気になる薬リストを誰にも預けない（公式は厚労省のサーバーに保持し、通知先メールもGビズIDに紐づく）
3. GビズIDの登録・ログインが要らない（〜エントリーが取れる以上、**差は「手間」だけで「可否」ではない**）

**★2と3を過大評価しないこと。** 現場の薬剤師にとって「メアドを厚労省に預けたくない」は
たいした動機にならない。**1が無ければこのアプリを作る理由は薄い。**

### 公開API（**認証なしで叩ける**・2026-08-17実測）

ベース `https://iyakuhin-kyokyu.mhlw.go.jp`。`Referer` を付けて呼んだ。**ドキュメントは無い。**

| エンドポイント | 内容 |
|---|---|
| `GET /api/info-site/supply-status-report?...` | 一覧。JSON。`total` と `data[]` |
| `GET /api/info-site/supply-status-report/history?yj_cd=<YJ>` | **★品目ごとの変化履歴** |
| `GET /api/info-site/supply-status-report/download?...` | Excel出力（＝xlsxと同じもの） |

主なクエリ：`yj_cd` `product_nm` `component_nm` `mfr_nm` `shipment_status`（`01`〜、カンマ区切り）
`publication_date_from` `publication_date_to` `page` `limit` `search_type=1` `sort` `order`

- `limit=1000` は通る。全件は `total=16385`（**うちのデータと完全一致**）
- 日付は `2026/08/17` でも `2026-08-17` でもよい

### ★`publication_date_from` が効く（この調査の最大の収穫）

`publication_date_from=2026/08/17` → **9件**。
うちの独自差分（`data/changes/2026-08-17.json`）の **9件とYJコードが完全一致**した。

> **つまり「今日変わったもの」を 6KB で直接引ける。** 1.48MBのxlsxを取って16,385行を突き合わせる必要がない。

- ただし **掲載終了（消えた品目）は拾えない**（消えたものは返ってこない）。新規掲載は拾える
- **一致の確認は1日ぶんだけ。** 日数を重ねて検証してから信用すること

### xlsxとの項目の差

| xlsxにあってAPIに無い | APIにあってxlsxに無い |
|---|---|
| ⑬⑫の更新日（※`history` にはある） | **`publication_date` 掲載日**（＝上記の差分検出に使える） |
| ⑮⑯解除見込み・在庫消尽時期（一覧APIには無い） | **`sales_company_nm_1〜3` 販売会社名** |
| ⑱⑲出荷量の改善見込み時期・量 | `reference_url` 参考URL |
| ⑳その他更新日／㉑New | `shipment_status`（コード `01`〜`05`） |

**★APIはxlsxの上位互換ではない。** 見込み時期（⑮⑯⑱⑲）が落ちるので、乗り換えると情報が減る。

- **ただし⑰出荷量はAPIにもある**（`shipment_volume_current_status_nm`＝`"A．出荷量通常"` と原本と同じ表記）。
  **公式が「持っているのに通知に使っていない」**ということ。データが無いのではなく、通知対象に入れていない

### 方針（2026-08-17 時点）

**★「足し算式」を採用（2026-08-17 決定・覆さない）。乗り換えない。**

- **xlsxを主データ源のまま維持する。** 公式に公表されたファイルであり、56項目の自己テストと
  列見出しの逐語照合（E-13）がそこに乗っている。**非公表APIに全体重を預けない**
- APIは**足し算として**使う：①カナリア（**実装済み**）②安価な更新検知（6KB）③販売会社名・参考URL ④履歴表示
- **★カナリアを置く → 2026-08-17 実装済み**（`scripts/lib/canary.mjs`・詳細は §3.5）。
  毎回 API の `total` と xlsx の行数、および掲載日の遅れを比べ、
  **xlsxだけ更新が止まったら気付ける**ようにした（これが最大のリスク対策）

**足し算の残り（未実装）：**

| # | 中身 | 効き目 | 前提 |
|---|---|---|---|
| ② | `publication_date_from` で「今日変わったもの」を6KBで引く | 1.48MBの取得を減らし、**取りに行く回数を増やせる** | **数日ぶんの一致検証がまだ**（1日ぶんのみ）。**掲載終了は拾えない**ので xlsx 併用は必須 |
| ③ | `sales_company_nm_1〜3` 販売会社名・`reference_url` | 「どこに聞けばいいか」が出せる | `build-site.mjs` の `SHIP` と索引の作り直し |
| ④ | `/history?yj_cd=` で品目ごとの変化履歴 | 「いつから限定出荷か」を**自前で貯めずに**出せる | 画面側の実装。PWAのCSPは `connect-src 'self'` なので**直接は叩けない**（★緩めない。取るならビルド時か Functions 経由） |
