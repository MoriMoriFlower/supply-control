# SupplyControl 固有指示（CLAUDE.md）

医薬品の供給不安を「通知」で受け取るPWA。
上位の `薬剤師/CLAUDE.md` とグローバル `~/.claude/CLAUDE.md` に**追加**で適用する。

- **リポジトリ：** <https://github.com/MoriMoriFlower/supply-control>（**public**・2026-08-16作成）
- **Cloudflareは未接続**（Step 2 で Git連携型として繋ぐ）

---

## 1. 承認済みサイト（再確認不要）

| サイト | 用途 | 承認日 |
|---|---|---|
| `www.mhlw.go.jp` | 供給状況一覧表xlsxの取得（一次情報源） | 2026-08-16 |

- **`drugshortage.jp`（DSJP）はアクセスしない。** WebFetchが403＝機械的アクセスを拒否している。一次情報の厚労省xlsxだけを使う
- 上記以外への外部アクセスは、グローバル規則2に従って**都度許可を得る**

---

## 2. 決定事項（★は覆さない）

- **★気になる薬リストはサーバーに送らない。** 端末内（IndexedDB）に置き、突合も通知文の組み立ても端末側でやる。
  サーバーが預かるのは**プッシュ購読情報（匿名文字列）だけ**。氏名・メール・所属・患者情報・薬リストは一切預からない
  - 理由と背景は共通ノウハウ **I-1**。採用薬リストは勤務先の調達情報にあたる
  - **これを台無しにする実装を足さない**：通知の開封計測／「該当◯件」のサーバー返送／エラーログへのリスト混入
  - **localStorageは使えない。** Service Workerは同期APIを呼べないため。画面側も同じIndexedDBを読む（共通ノウハウ C-6 周辺）
- **★リストの呼称は「気になる薬」。**「採用薬」と呼ばない（I-1）。ただし呼び方でリスクは減らないので、端末内完結の設計は厳守
- **★重い処理はGitHub Actions、Cloudflareは配るだけ。** CPU 10msでは16,384行のxlsx解析は絶対に終わらない（共通ノウハウ A-11）
- **★既存の検索アプリには組み込まない。** 更新頻度も配信方式も性格も違う。連携（YJコードで飛ばす／リストの受け渡し）は後付けする
- **★通知は「変化した事実」だけを伝え、判断は一次情報リンクに飛ばす。** 在庫・代替・処方変更について何も言わない
- **合言葉はつけない。ただし公開までは検索避け**（`robots.txt` / `<meta name="robots">` / `_headers` の `X-Robots-Tag`）。
  公開に踏み切るときは**3箇所とも外す**必要がある
- **リポジトリはpublic。** 秘密（VAPID秘密鍵・Cloudflare APIトークン）は必ず GitHub Secrets。コードにも `data/` にも置かない

---

## 3. 元データについて（2026-08-16 実測）

出典：厚労省「医療用医薬品の供給状況」ページ → `.xlsx` 直リンク1本。

| 項目 | 実測値（令和8年8月14日版） |
|---|---|
| シート | 「供給状況一覧表」1枚のみ |
| 行 | ヘッダーR02、データR03から。**16,384行** |
| 列 | 21列 |
| ⑤YJコード | **空0件・重複0件**＝主キーとして使える |
| ⑫出荷対応 | 通常14,542 / 供給停止873 / 限定出荷(自社)479 /(他社影響)416 /(その他)74 |
| ⑰出荷量 | 通常10,711 / 増加3,457 / 減少1,044 / 薬価削除予定699 / 出荷停止473 |
| ファイルサイズ | 1.48MB |

### ⑫出荷対応 × ⑰出荷量（2026-08-16 実測・令和8年8月14日版）

**この2列は独立している。片方だけ見ると見落とす。**

| ⑫出荷対応 | ⑰出荷量 | 件数 |
|---|---|---|
| ①通常出荷 | A．通常 | 10,240 |
| ①通常出荷 | Aプラス．増加 | 3,205 |
| **①通常出荷** | **B．減少** | **827** |
| ①通常出荷 | D．薬価削除予定 | 270 |
| ⑤供給停止 | C．出荷停止 | 473 |
| ⑤供給停止 | D．薬価削除予定 | 400 |
| ②③④限定出荷 | A．通常 ／ Aプラス．増加 | 723 |
| ②③④限定出荷 | B．減少 | 217 |
| ②③④限定出荷 | D．薬価削除予定 | 29 |

- **★出荷量減少1,044件のうち827件（79%）は「①通常出荷」。** メーカーの回答は通常出荷でも、量は減っている。
  **⑫だけで「調整中」を判定すると、現場で一番効くこの層が丸ごと抜ける**
- **①通常出荷なのに薬価削除予定が270件。** 「今は普通に来るが、いずれ無くなる」もの
- **C．出荷停止(473)は全件⑤供給停止**＝⑰Cは⑫⑤の部分集合。ここだけは重なっている

### 罠（踏んだもの／踏みかけたもの）

- **㉑「今回掲載時の更新有無(New)」は差分検出に使えない。** 実測でNewは3件のみ。「新規掲載」の意味であって「ステータス変化」ではない → 共通ノウハウ **F-7**
- **⑬「⑫を更新した日」は使える。** 実測3,446件に日付が入っており、令和7年5月13日以降の変化履歴が読める。
  **ただし「最後に更新した日」なので上書きされる**＝古い日ほど過小評価。日次の完全な差分は自前で貯めるしかない
- **ファイル名の数字は西暦の下2桁。** `260814iyakuhinkyoukyu.xlsx` = 2026-08-14。
  和暦（令和8年）ではない。`resolveSource()` はリンク文言の和暦とファイル名の西暦を**両方読んで一致を検証**し、食い違えば止める
- **YJコードが12桁でないものが404件ある。** すべて `X00000` 形式の未収載医薬品（液化酸素などの医療ガス）で、全件「①通常出荷」。
  **既存の検索アプリ（PMDA電子添文＝12桁YJ）とは結合できない**。異常ではないので止めないが、件数を `meta.json` に残している
- **厚労省は最新版1本しか置かない。** 過去分のリンクは無い → git履歴が唯一の過去データ（F-7）
- **土日は更新されない。** ⑬の記録291日ぶんに土日は1日も無い
- **★ただし「毎営業日」でもない。** 直近90日の平日65日のうち、⑬に記録があるのは**48日＝74%**。
  お盆は特に止まる（2026年は 8/10月=2件・8/12水=1件・8/14金=3件。8/11は山の日、8/13は記録なし）。
  **「今日は変化なし」は障害ではなく通常。** 数日コミットが出なくても壊れたと判断しない
  （⑬は上書きされるので74%は下限。実際の更新日はもう少し多い可能性がある）

### 1日あたりの変化件数（⑬から推定・2026-08-16時点）

直近3か月で **中央値12件／平均11.8件／最大54件**（記録がある日のみ・平日のみ）。曜日別では木曜が多い（平均21.6件）。
**⑬は上書きされるため下限寄りの推定**。運用開始後に実測値へ置き換えること。

---

## 3.5 ファイル構成と、配るもの／配らないもの（2026-08-16 Step 2 で追加）

```
scripts/collect.mjs      厚労省xlsx取得 → items.json / status.json / changes/ / meta.json
scripts/build-index.mjs  保存済みJSON → search.json / status-lite.json（外部アクセス不要）
scripts/build-site.mjs   dist/ を組み立てる（pwa/ ＋ 画面が使う data/ だけ）
scripts/dev.mjs          手元で見るための静的サーバー（127.0.0.1:8787）
pwa/                     画面。素のESM・依存ライブラリなし
```

| コマンド | すること |
|---|---|
| `npm run collect` | 厚労省から取得（**唯一の外部アクセス**） |
| `npm run index` | 索引を作り直す。外部アクセスなし・いつでも実行可 |
| `npm run selftest` | 差分・検索・索引の自己テスト（外部アクセスなし・35項目） |
| `npm run build` | `dist/` を組み立て |
| `npm run dev` | build してローカル配信 → <http://localhost:8787> |

**Cloudflare Pages（Git連携型）の設定：** ビルドコマンド `npm run build` ／ 出力ディレクトリ `dist`

### 配布物の役割（★分けている理由を崩さない）

| ファイル | 生 | brotli | 変わる頻度 | 配信 |
|---|---|---|---|---|
| `search.json` 品目の素性 | 2,242KB | 196KB | **まれ**（新規収載・掲載終了のときだけ） | する |
| `status-lite.json` 出荷対応・出荷量（コード） | 350KB | 42KB | 毎日 | する |
| `meta.json` 版と各ファイルのsha256 | 1.6KB | 0.7KB | 毎日 | する |
| `changes/*.json` 変化した品目 | 数KB | — | 毎日 | する |
| `items.json` / `status.json` 原本の全列 | 5.6MB | 320KB | 毎日 | **しない** |

- **★索引に版（`asOf`）を書かない。** 書くと中身が同じ日でもsha256が変わり、
  2.2MBを毎日取り直すことになる。版は `meta.json` が持つ（共通ノウハウ **A-13**）
- **PWAは `meta.json` のsha256を見て、変わっていなければ端末の控え（IndexedDB）を使う。**
  ふだんの起動は数十KBで済む
- **`items.json` / `status.json` は配らない。** 画面が読まないため。
  理由・見込みまで表示する画面を作るときに、そのとき必要なものを `build-site.mjs` の `SHIP` に足す。
  git履歴には残り続けるので失われはしない
- **「重い」と言う前に圧縮後を測る。** 日本語JSONはbrotliで16分の1になる（共通ノウハウ **A-12**）

### 検索の正規化（`pwa/js/normalize.js`）

原本は全角（`ラボナール注射用０．３ｇ`）。NFKC →小文字→カタカナをひらがなへ→小書きを大書きへ→記号と長音を除去、で突き合わせる。
実測で `ラボナール 0.3g` `らぼなーる` `ﾑｺﾀﾞｲﾝ` `ろきそにん錠60` すべて当たる。

- **既知の副作用：** 記号を落とすので `2.5mg` が `25mg` になり、`5mg` で検索すると当たる。
  **消さずに並べ替えで救っている**（品名の先頭一致＞品名に含む＞短い順）。利用者は一覧から原本の品名を見て選ぶので、誤った情報にはならない
- 項目の区切りは `|`。正規化で必ず消える文字なので、検索語には現れない＝項目をまたいだ誤ヒットが起きない

### しぼり込み（`pwa/js/app.js` の `FILTERS`）

さがすタブで、語を入れずにボタンだけ押すと一覧できる（品名順・100件ずつ）。件数はボタン上に出る。検索語との併用も可。

- **★「調整中」は厚労省の⑫出荷対応の ②③④⑤ をまとめただけ**（限定出荷3種＋供給停止）。
  こちらで独自の基準を作らない。**①通常出荷を調整中に含めない**。実測 1,842件（限定出荷969・供給停止873）
- **★「出荷量減少」は⑰出荷量のB＝別の列**なので、ボタンも仕切って分けてある。
  **1,044件のうち827件は⑫が①通常出荷**＝調整中には出てこない（下のクロス集計）。
  この2列を1つのボタンにまとめない

---

## 4. 実装の掟

- **列構成が変わったら黙って動かない。** `scripts/lib/schema.mjs` に原本の見出しを逐語で持ち、毎回突き合わせて不一致なら例外で止める。
  「別の列を保存し続ける」ほうが「止まる」より遥かに悪い（共通ノウハウ E-13）
- **不可視文字（NUL・ゼロ幅・BOM）は正規化の最初に落とす。** ソースコードには**エスケープ表記でしか書かない**（直接埋めると目視で判別できない）→ E-7
- **`data/*.json` は1行1レコードで書く。** gitのdiffが「変わった品目の行だけ」になり、履歴が軽くなる
- **差分の並び順を固定する。** 順序が揺れると、中身が同じでも毎日diffが出てコミットが無駄に増える
- **`npm run selftest` は外部アクセス不要**で動く。差分ロジックを触ったら必ず実行する（Actionsでも毎回走る）
- **ソースに不可視文字を直接埋めない。** 実際に `normalize.js` へ U+0001 が2つ混入した（2026-08-16）。
  目視では絶対に見つからないので `npm run selftest` が機械で検査している（共通ノウハウ **E-7補**）
- **`pwa/` に外部ライブラリを入れない。** 素のESMだけで書く。
  `_headers` の CSP を `default-src 'self'` / `connect-src 'self'` に絞ってあり、外部への通信は仕組みとして塞いである。
  ★これは「気になる薬リストが外へ出る経路を作らない」ための担保でもあるので緩めない

---

## 5. 未確認・これから確かめること

- **iOSのWeb Push実機検証**（共通ノウハウ C-6。仕様調査のみで実機未検証）
  - ホーム画面に追加したPWAでのみ届くか
  - 許可ダイアログがタップ起点でのみ出るか
  - **該当0件の日に沈黙できないという想定が本当か**（`userVisibleOnly` の実挙動）
- 新システム `iyakuhin-kyokyu.mhlw.go.jp/public/supply-status-list`（令和8年3月31日稼働）にAPI/CSVがあるか。
  SPAのため未取得。**現行xlsxが廃止される可能性があるので、いずれ確認が要る**
- 元ファイルが実際に更新される時刻（Last-Modifiedの観測を貯めて、cronの時刻を最適化する）
  - **観測1件目：** 2026-08-14版 = `Fri, 14 Aug 2026 05:18:41 GMT` ＝**JST 14:18**
  - **観測2件目：** 2026-08-17版 = `Mon, 17 Aug 2026 08:11:16 GMT` ＝**JST 17:11**
  - **★公開時刻は日によって3時間ずれる（14:18 / 17:11）。「毎日◯時」と決め打ちできない。**
    JST 15:20 の回だけなら 8/17 は取り逃していた＝**1日4回に増やした判断は正しかった**
- **★GitHubのスケジュールは指定時刻に走らない前提で組む。** 2026-08-17 の実測で、
  `cron: '10 7 * * *'`（JST 16:10）に対し**実際の起動は JST 16:58＝48分遅れ**。
  混雑時は遅延・スキップがある。だから1日4回に増やして回数で吸収している
  （publicリポジトリのActionsは無料・1回17秒・変化なしならコミットしないので、増やす副作用がない）
- **⚠ 通知が突然来なくなったら、まずここを疑う：GitHubは「リポジトリに60日間活動がない」とスケジュール実行を自動停止する。**
  本プロジェクトはほぼ毎日コミットが出る想定なので通常は当たらないが、
  **bot（github-actions）のコミットが「活動」として数えられるかは未確認**。
  止まっていたら Actions 画面に再有効化ボタンが出る（`workflow_dispatch` の手動実行でも復帰する）
